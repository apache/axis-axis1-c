#set echo
foreach i (*) 
  if (-d $i) then 
    if ($i != "CVS") then
      echo adding directory $cwd/$i
      cvs add $i
      (cd $i; tcsh c:/projects/xml-axis/contrib/doit)
    endif
  else
    echo adding file $cwd/$i
    cvs add $i
  endif
end
